apiVersion: v2
name: cilium
type: application
version: 1.20.1
appVersion: 1.20.1
dependencies:
  - name: cilium
    version: "1.20.1"
    repository: https://helm.cilium.io/
# templates/crds.yaml ships the Gateway API (standard channel) CRDs, pinned to
# v1.6.1 to match this cilium version's sigs.k8s.io/gateway-api pin. Bump the
# CRDs together with cilium.