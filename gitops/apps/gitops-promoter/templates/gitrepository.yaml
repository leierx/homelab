apiVersion: promoter.argoproj.io/v1alpha1
kind: GitRepository
metadata:
  name: homelab
  namespace: promoter-system
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  github:
    name: homelab
    owner: leierx
  scmProviderRef:
    name: homelab