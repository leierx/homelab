apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gitops-promoter
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io/background
spec:
  project: default
  sourceHydrator:
    drySource:
      repoURL: https://github.com/leierx/homelab.git
      path: gitops/apps/gitops-promoter
      targetRevision: HEAD
      helm:
        valueFiles:
          - values.yaml
          - prod-values.yaml
    syncSource:
      targetBranch: env/prod
      path: gitops/hydrated/gitops-promoter
    hydrateTo:
      targetBranch: env/prod-next
  destination:
    server: https://kubernetes.default.svc
    namespace: promoter-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true